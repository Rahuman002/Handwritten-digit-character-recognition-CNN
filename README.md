# Handwritten-digit-character-recognition-CNN
This is a Handwritten Digit and Character Recognition Application based on the CNN model. It will be able to process the input image, extract handwritten text and convert it into text. The project applies Deep Learning techniques and image preprocessing to efficiently detect an handwritten characters and digits
